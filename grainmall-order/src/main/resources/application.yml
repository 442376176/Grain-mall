spring:
  application:
    name: grainmall-order
  datasource:
    username: root
    password: root
    url: jdbc:mysql://127.0.0.1:3306/grain_mall_order
    driver-class-name: com.mysql.jdbc.Driver
mybatis-plus:
    #classpath*  *包括引用包的路径
  mapper-locations: classpath*:/mapper/**/*.xml
  # 自增主键
  global-config:
    db-config:
      id-type: auto

server:
  port: 9000